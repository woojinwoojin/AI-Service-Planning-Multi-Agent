# 실 LLM 표본 측정 (N=7) — 2026-07-28

지금까지 발표 수치는 **주제 1개 실측**에 기대고 있었다. 그러면 "27/28"이 재현되는 값인지 한 번의
운인지 구분할 근거가 없다. 그래서 서로 다른 도메인 주제로 반복 측정해 **분포**를 만들었다.

## 측정 조건

| 항목 | 값 |
|---|---|
| 모델 | `gpt-4o-mini` (OpenAI) · 심판도 동일 |
| 실행 구조 | `WORKFLOW_MODE=parallel` (배포와 동일) |
| 코드 | 동결 커밋 `f0c4739`(#130)와 생성·판정 로직 동일 |
| 표본 | **7건** = 신규 5개 주제 + 같은 조건의 기존 2건(시각장애인 웨어러블 로컬·배포) |
| 주제당 실행 | **1회** (⚠️ 아래 '한계' 참고) |
| 검색 | Tavily 정상(쿼터 교체 후) |
| 총비용 | 약 **$0.15** |

주제는 **데이터 가용성이 다르게** 골랐다 — 결과가 주제에 따라 얼마나 흔들리는지 보는 것이 목적이다:
B2B SaaS(소상공인 재고) · 소비자앱(반려동물 행동) · 커머스(중고의류) · 헬스케어 규제(고령자 복약) ·
산업 니치(제조 안전) · 접근성 하드웨어(시각장애인 웨어러블 ×2).

## 주제별 원자료

| 주제 | KOSENA | 미충족/부분 | 근거 | 확인율 | 점수 | 비용 | 소요 | 채택본 |
|---|---|---|---|---|---|---|---|---|
| 소상공인 재고·발주 예측 | 26/28 | `competitors_3_2_1`·`doc_length` | 18 | 0.20 | 67 | $0.0216 | 178초 | revised |
| 반려동물 이상행동 감지 | 26/28 | `competitors_3_2_1`·`doc_length` | 18 | 0.20 | 61 | $0.0223 | 164초 | revised |
| 중고 의류 리커머스 | 26/28 | `competitors_3_2_1`·`doc_length` | **9** | 0.60 | 65 | $0.0162 | 148초 | **draft** |
| 고령자 복약 관리 | **27/28** | `doc_length` | 19 | 0.20 | **75** | $0.0239 | 172초 | revised |
| 제조 현장 안전 감지 | 26/28 | `competitors_3_2_1`·`doc_length` | 17 | 0.60 | 70 | $0.0213 | 159초 | revised |
| 시각장애인 웨어러블 (로컬) | **27/28** | `doc_length` | 17 | 0.60 | 64 | $0.0223 | 171초 | **draft** |
| 시각장애인 웨어러블 (배포) | **27/28** | `doc_length` | 19 | 1.00 | 69 | $0.0236 | 168초 | revised |

`run_status` 는 **7/7 전부 `success`** (폴백 0 · 실패 노드 0).

## 집계

| 지표 | 평균 | 중위 | 범위 | 표준편차 |
|---|---|---|---|---|
| KOSENA 충족 | **26.4/28** | 26 | **26~27** | 0.5 |
| 근거 건수 | 16.7건 | 18 | 9~19 | 3.5 |
| 비용 | **$0.0216** | $0.0223 | $0.0162~0.0239 | $0.0026 |
| 토큰 | 104,190 | 108,345 | 69,292~119,647 | 16,617 |
| LLM 호출 | 18.4회 | 19 | 17~19 | 0.8 |
| 소요 | **166초** | 168 | 148~178 | 10 |
| 근거 확인율 | 0.49 | 0.60 | **0.20~1.00** | **0.30** |
| 심판 점수 | 67.3 | 67 | 61~75 | 4.6 |

## 읽어야 할 다섯 가지

### 1. KOSENA 는 26~27/28 이고 27 이 기본값이 아니다

7건 중 **27/28 은 3건, 26/28 은 4건**이다. 그래서 `27/28` 을 단일 대표값으로 말하면 표본이
지지하지 않는다. 정확한 표현은 **"26~27/28, 평균 26.4"** 다.

### 2. 반복적으로 걸리는 항목은 두 개뿐이고, 성격이 다르다

| 항목 | 빈도 | 성격 |
|---|---|---|
| `doc_length` | **7/7** | **구조적** — 요건 30~50쪽 vs 실측 약 11~13쪽. 분량을 위한 분량을 넣지 않기로 판단한 결과 |
| `competitors_3_2_1` | **4/7** | **LLM 출력 편차** — 직접 경쟁사 3개를 요구하는데 2개만 생성하는 경우 |

`competitors_3_2_1` 이 26 과 27 을 가르는 유일한 변수다. 즉 **KOSENA 값의 흔들림은 시스템 품질
변동이 아니라 "모자란 개수를 지어내 채우지 않는다"는 원칙(KOSENA p4)이 그대로 드러난 것**이다.
빈칸을 채워 통과시키면 28/28 이 되지만 문서에는 빈 경쟁사가 남는다.

### 3. KOSENA Agent 완주율은 4종 전부 7/7

`kosena_industry` · `kosena_model` · `kosena_research` · `kosena_roadmap` 모두 7/7 로 산출물을
만들었다.

> ⛔ **2026-07-28 정정 — "3개가 빈 결과를 낸 사건"의 원인을 찾았다.**
> 이 문서는 원래 그 사건을 *"같은 조건 7회에서 재현되지 않았으니 **드문 일시적 실패**로 보는 편이
> 타당하다"* 고 적었다. **그 판단은 틀렸다.** 원인은 코드 결함이고 **100% 결정적**이다.
>
> `apply_node_update()`(`app/graph/workflow.py`)는 그래프 **밖**에서 노드 결과를 합치는데,
> reducer 필드를 손으로 나열한다. 그 목록에 **`kosena` 가 빠져 있어서** `dict.update` 가
> 덮어썼고, KOSENA Agent 4개가 의도적으로 '자기 키만' 반환하므로 **마지막 노드
> (`kosena_roadmap`)의 10개 키만 남았다**(28개 중 18개 소실 → M1·M2 붕괴).
>
> **경로에 따라 갈렸다**: `/run`(`graph.invoke`)은 reducer 가 적용돼 정상,
> **`/run/stream`(UI 버튼이 쓰는 경로)만 깨졌다.** 이 표본 7건은 전부 스크립트(`run_workflow`)로
> 측정했기 때문에 정상이었고, 사건은 전부 브라우저로 실행한 경우였다. **"로컬 7회 정상 · 배포
> 2회 실패"는 환경 차이가 아니라 코드 경로 차이였다.**
>
> 더미 모드 재현: invoke 28키(26/28) vs stream 10키(**11/28**) — 이전 사건의 11/28 과 일치한다.
> 수정 후 실 LLM 로 `/run/stream` 관통 검증: **28키 · 27/28 · 미충족 0**.
> 수정·회귀 테스트는 `tests/test_stream_reducer_parity.py`, 사유는 [`동결_기록.md`](동결_기록.md).
>
> **이 표본의 수치(26~27/28)는 그대로 유효하다** — 측정 경로(`run_workflow`)가 결함 경로가
> 아니었고, Agent·프롬프트·판정 로직은 손대지 않았다. 다만 **"UI 로 실행해도 같은 값이 나온다"는
> 것은 이 수정 이후에야 사실이 되었다.**

### 4. 품질 게이트는 7/7 모두 미통과 — 결함이 아니라 기준이 엄격한 것

| 차단 사유 | 빈도 | 임계값 | 실측 |
|---|---|---|---|
| `score` | 7/7 | ≥ 80점 | 61~75 (평균 67.3) |
| `major_issues` | 7/7 | ≤ 1건 | 초과 |
| `evidence` | 6/7 | 근거 충족률 ≥ 0.8 | 0.20~1.00 |

**`release_ready=True` 가 한 건도 없다.** 이것은 게이트가 **작동한다**는 뜻이고, 동시에 **현재
파이프라인 산출물이 자동 출판 기준에는 못 미친다**는 뜻이다. 게이트 임계값은 `calibrated: False`
(사람 기준선 보정 전 잠정값)이므로, "80점을 넘지 못했다"를 곧 "품질이 낮다"로 읽으면 안 된다 —
**기준선이 아직 교정되지 않았다.**

### 5. 근거 확인율은 주제 간에도 크게 흔들린다 (0.20~1.00)

표준편차 0.30 이다. 이미 기록된 관측(같은 문서를 9회 재판정하면 `fact_support_rate` 가 0.2~0.9 로
벌어진다 — verify 가 주장을 매번 새로 추출)이 **주제를 바꿔도 같은 폭으로 나타난다.**
그리고 이 값이 게이트의 `evidence` 판정에 쓰이므로 **게이트 결과에 운이 섞인다.** 미해결 과제다.

> 부수 관찰: 주장 수가 7건 중 6건에서 정확히 5건이었고 1건만 1건이었다. 추출되는 주장 수 자체가
> 적어, 확인율이 한 주장 차이로 0.2 씩 움직인다(1/5 = 0.2). 분모가 작은 비율이라 해석에 주의.

### 그 밖에

- **채택본**: `revised` 5건 · **`draft` 2건**. 재작성본이 초안보다 낮게 평가돼 **되돌린 경우가
  7건 중 2건(29%)** 이다 — "AI 응답을 그대로 쓰지 않는다"의 실측 근거. 되돌린 2건은
  **중고 의류 리커머스**(초안 65 = 최종 65)와 **시각장애인 웨어러블(로컬)**(64 = 64)이다.

  > **2026-07-29 정정** — 위 표의 '채택본' 열이 두 행에서 원자료와 어긋나 있었다: 중고 의류를
  > `revised`, 웨어러블(배포)를 `draft` 로 적었다. 실제로는 **중고 의류가 `draft`
  > (`reverted_from_revision: true`), 웨어러블(배포)가 `revised`** 다. `presentation-assets/
  > metrics/sample-n7.json` 과 실행 State 원본(`projects.db` id 11·7, `deployed-00009-run.json`)을
  > 직접 대조해 고쳤다. **되돌린 건수 2건(29%)과 다른 모든 집계는 영향받지 않는다** — 어긋난 것은
  > 어느 주제가 되돌려졌는지였다.
- **`retrieved_at`**: 근거가 있는 7건 **전부 전건 기록**(117/117).
- **`published_date`**: 117건 중 25건(21%)만. 검색이 줄 때만 채우고 없으면 '미상' 으로 두므로
  **낮은 것이 정상**이다.
- **근거 건수 편차**: 중고의류만 9건으로 낮다(다른 주제는 17~19건). 검색 결과 수는 주제에 따라
  달라지며, 근거가 적어도 파이프라인은 완주했다.

## ⚠️ 이 표본의 한계 (반드시 함께 말할 것)

1. **주제당 1회 실행이다.** 그래서 **주제 간 차이와 실행 간 편차가 분리되지 않는다.**
   `competitors_3_2_1` 이 4/7 인 것이 "그 주제는 경쟁사가 2개뿐"인지 "같은 주제도 실행마다
   흔들리는지" 구분할 수 없다. 분리하려면 같은 주제를 반복 실행해야 한다(비용 배수).
2. **N=7 은 작다.** 평균을 정밀 추정치로 제시하지 말고 **범위와 함께** 말한다.
3. **모델 1종·언어 1종·심판 1종**이다. 모델을 바꾸면 값이 달라질 수 있다.
4. 시각장애인 웨어러블 2건은 **같은 주제**이므로 주제 다양성은 실질 6종이다.
5. 심판이 LLM 이다(`gpt-4o-mini`). 점수는 절대 척도가 아니다.

## 발표에서 쓸 문장

### ✅ 쓸 수 있는 것

> **"서로 다른 7건의 실 LLM 실행에서 KOSENA 준수는 26~27/28(평균 26.4)이었고, 미충족은 전부
> 두 항목 — 문서 분량과 직접 경쟁사 개수 — 에 몰렸습니다."**

> **"경쟁사 항목이 갈리는 이유는 모자란 개수를 지어내 채우지 않기 때문입니다. 빈칸을 채우면
> 28/28 이 되지만 문서에는 빈 경쟁사가 남습니다. 그래서 준수율이 흔들리는 것을 그대로 뒀습니다."**

> **"실행당 비용은 평균 $0.022(범위 $0.016~0.024), 소요 166초, LLM 호출 18회입니다. 7건 측정
> 기준입니다."**

> **"7건 모두 완주했고 폴백은 0건입니다. KOSENA Agent 4종의 완주율도 7/7 입니다."**

> **"모든 근거에 조회 시점을 기록합니다 — 7건의 근거 117건 전부에 `retrieved_at` 이 있습니다.
> 발행일은 검색이 줄 때만 채우므로 21%이고, 모르는 것은 '미상'으로 둡니다."**

> **"품질 게이트는 7건 모두 미통과입니다. 게이트가 작동한다는 뜻이고, 동시에 자동 출판 기준에는
> 아직 못 미친다는 뜻입니다. 임계값은 사람 기준선 보정 전 잠정값입니다."**

> **"재작성본이 초안보다 낮게 평가되면 초안을 되돌립니다. 7건 중 2건에서 실제로 되돌렸습니다 —
> AI 응답을 그대로 쓰지 않는다는 가장 분명한 증거입니다."**

> **"근거 확인율은 0.20~1.00 으로 크게 흔들립니다. 검증 Agent 가 주장을 매번 새로 추출하기
> 때문이고, 이 값이 게이트에 쓰이므로 게이트 판정에 운이 섞입니다. 미해결 과제로 기록했습니다."**

### ❌ 쓰면 안 되는 것

| 하지 말 것 | 왜 |
|---|---|
| "KOSENA 27/28 을 달성했습니다" | 7건 중 3건만 27. 표본은 **26~27** 을 지지한다 |
| "26/28 에서 27/28 로 개선했습니다" | 개선이 아니라 경쟁사 개수 편차다. 코드는 동일 |
| "28개 항목을 모두 충족했습니다" | `doc_length` 는 7/7 미달 |
| "품질 게이트를 통과합니다" | **0/7 통과.** 게이트는 막는 쪽으로 작동했다 |
| "각 KOSENA 주장마다 출처를 검증했습니다" | 출처 검사는 실행 전체 존재 검사. 항목별 연결은 미구현 |
| "근거 확인율 평균 0.49 입니다"(단독) | 분모가 5건뿐이라 한 주장에 0.2 씩 움직인다. 범위와 함께 말할 것 |
| "실행당 $0.022 입니다"(단정) | 범위 $0.016~0.024. 주제에 따라 다르다 |
| "모든 Agent 가 항상 완주합니다" | 표본 7/7 은 사실이지만 **완주 ≠ 결과 보존**이었다. Agent 는 완주하는데 산출물이 State 병합에서 사라진 결함이 있었다(위 정정 참고, 수정 완료) |
| "이 결함은 드물게 발생합니다" | **결정적이었다.** UI 경로는 매번 깨졌다. '드물게'로 서술하면 원인을 모른 채 넘기는 것이 된다 |

### 예상 질문 대비

- **"왜 26 과 27 이 섞이나요?"** → 직접 경쟁사 3개 요구를 LLM 이 2개만 채우는 경우가 있고,
  지어내 채우지 않으므로 부분 충족으로 보고됩니다. 7건 중 4건이 그랬습니다.
- **"품질 게이트를 통과 못 하는데 쓸 수 있나요?"** → 게이트는 **자동 출판** 기준입니다. 사람이
  검토·보완하는 초안으로는 쓸 수 있고, 게이트가 무엇이 부족한지(점수·major 이슈·근거 충족률)를
  항목으로 알려줍니다. 임계값 자체가 사람 기준선 보정 전 잠정값입니다.
- **"표본 7건으로 충분한가요?"** → 충분하지 않습니다. 주제당 1회라 주제 차이와 실행 편차가
  섞여 있고, 분리하려면 같은 주제 반복 측정이 필요합니다. 현재는 **분포의 폭**을 보는 용도입니다.

## 원자료

`presentation-assets/metrics/sample-n7.json` — 주제별 지표 전체(스크립트 출력 그대로).
실행 State 원본은 저장 용량 때문에 백업 2건만 커밋했다(`presentation-assets/demo-results/`).
