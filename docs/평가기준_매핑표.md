# 평가 기준 ↔ 구현 매핑표

> 과제 평가 기준(체크포인트)이 코드의 **어디로** 구현됐는지, 그리고 **무엇이 아직 아닌지**를
> 한 장에 모았다. 근거는 전부 파일·함수 단위로 지목한다.
> 갱신: 2026-07-27 · 테스트 644 passed · 실 LLM 실측 1주제(gpt-4o-mini)

**이 문서의 원칙**: 구현한 것과 앞으로 할 것을 섞지 않는다. `✅ 구현`·`🟡 부분`·`❌ 미구현`으로만
적고, 부분·미구현에는 **왜 그 상태인지**를 함께 쓴다. 과장은 평가에서 가장 비싸다.

---

## 체크포인트 1 — Multi-Agent 구조

| 요구 | 상태 | 구현 위치 | 비고 |
|---|---|---|---|
| 복수 Agent 분업 | ✅ | `app/graph/workflow.py` — **22개 노드** | 분석 7 + KOSENA 4 + 문서·검토 등 |
| Agent 간 의존 관계 | ✅ | `app/schemas/artifact.py` `depends_on` | 선언이 아니라 **검증된 사실** — `test_declared_depends_on_matches_actual_runtime_reads` 가 실제 `artifact.read` 호출과 대조 |
| 오케스트레이션 | ✅ | LangGraph `StateGraph` | 직렬·병렬 두 그래프 |
| 조건부 분기 | ✅ | `_route_revision` 3분기 | 섹션 수정 / 전체 재작성 / 종료 |
| 병렬 실행 | ✅ | `WORKFLOW_MODE=parallel` | 독립 4분기 동시 실행 후 Draft 합류 |
| 실패 격리 | ✅ | `_safe()` 래퍼 | 한 Agent 실패에도 관통, `fallback_nodes` 에 정직 표기 |
| 동적 위임 | 🟡 | `research_gap` 노드 | Research 가 보고한 **근거 공백에만** 추가 조사(상한 2). 임의 위임은 아니다 |
| 자율 계획 수립 | ❌ | — | 그래프가 고정이다. "자율 multi-agent"라고 말하지 않는다 |

## 체크포인트 2 — 생성형 AI 활용

| 요구 | 상태 | 구현 위치 | 비고 |
|---|---|---|---|
| 실제 LLM 사용 | ✅ | `app/services/llm.py` | 실측 모델 ID = **`gpt-4o-mini`**(OpenAI). Anthropic provider 도 지원 |
| 구조화 출력 강제 | ✅ | 각 Agent `_validate()` | 누락 키는 중립값. **모자란 개수를 지어내 채우지 않는다** |
| 프롬프트 설계 | ✅ | `app/prompts/templates.py` | KOSENA 4종은 표준 5단 구조(`[역할][입력][요구사항][출력 형식][검증 조건]`) — 테스트로 고정 |
| 웹 검색 grounding | ✅ | `app/services/search.py` (Tavily) | 키 없으면 검색 생략(LLM 지식 기반) |
| 근거 추적성 | ✅ | `app/services/evidence.py` | `evidence_registry` — URL 중복 제거 + `evidence_id` 부여 + 주장 역인덱스 |
| 사실 검증 | 🟡 | `app/agents/verifier.py` | **검색 요약 기준**이다. URL 원문은 재검증하지 않는다(아래 '한계') |
| 재시도·폴백 | ✅ | `llm.complete_json` 1회 재시도 | 실패는 예외로 안 터뜨리고 `status` 에 정직 표기 |
| 심판 모델 분리 | ✅ | `reviewer._reviewer_model` | `REVIEWER_MODEL` 로 생성/채점 모델 분리(자기 채점 편향 완화) |
| AI 응답 무비판 사용 방지 | ✅ | `_select_best` | 재작성본이 초안보다 낮으면 **초안으로 되돌린다**(`best_version`) |

## 체크포인트 3 — KOSENA 방법론 준수

상세는 [`kosena-compliance.md`](kosena-compliance.md). 요약:

| 요구 | 상태 | 구현 위치 |
|---|---|---|
| 28개 요구항목 자체점검 | ✅ | `app/services/kosena.py` — 결정적·LLM 없음·근거 쪽수 표기 |
| M1 산업·비즈니스 모델 | ✅ 8/8 | `kosena_industry` · `kosena_model` |
| M2 데이터 기반 기획 | ✅ 10/10 | `kosena_research` · `kosena_roadmap` |
| M3 개발 로드맵 | ✅ 6/6 | `kosena_roadmap` |
| 7종 산출물 문서 | ✅ | `app/services/kosena_doc.py` |
| DOCX · PPTX 내보내기 | ✅ | 기존 익스포터 재사용(markdown 만 받는 순수 함수) |
| AI 활용 로그 | ✅ | `app/services/ai_log.py` — 프롬프트·입력·산출·검증·채택 여부 |
| 발표자료 15~20쪽 | ✅ **20쪽** | 생성된 PPTX 의 실제 슬라이드 수를 테스트가 센다 |
| 본문 A4 30~50쪽 | 🟡 **약 11.9쪽** | 아래 '알려진 한계' 참고 |
| 출처 명시 | ✅ | 실 LLM 실측 **16건**. ⚠️ 범위 = 실행 전체(항목별 연결 아님) |

**실 LLM 실측(gpt-4o-mini, 1주제)**: **26/28 충족 · 부분 2 · 미충족 0** · `data_source: real`.
부분 2건 = 직접 경쟁사 2개만 생성(3개 요구) · 본문 분량.

---

## 품질·운영

| 요구 | 상태 | 구현 위치 | 비고 |
|---|---|---|---|
| 자동 테스트 | ✅ | `tests/` — **644개** | 실 LLM 호출 없이 결정적. CI 커버리지 하한 90% |
| CI | ✅ | `.github/workflows/ci.yml` | 4잡: ruff+pytest · gitleaks · pip-audit · docker build |
| main 브랜치 보호 | ✅ | 저장소 설정 | 위 4개 체크 필수. main 직접 push 불가 |
| 컨테이너화 | ✅ | `Dockerfile` (비루트 uid 10001) | CI 에서 빌드 검증 |
| 배포 | 🟡 | `scripts/deploy_cloudrun.sh` · GCP Cloud Run | **수동 실행**이다. Actions 승인형 CD 는 아래 '미구현' |
| 공개 배포 안전장치 | ✅ | `app/services/public_guard.py` | IP 빈도 + 전역 일일 실행수·비용 상한. 기본 전부 비활성 |
| 출력 가능 여부 게이트 | ✅ | `app/services/quality_gate.py` | 총점·치명이슈·서식·근거충족·반대근거 |
| 관측성 | ✅ | `usage.py` · `timing.py` | 토큰·추정비용·지연·stage wall·critical path |
| 예산 상한 | ✅ | `app/services/budget.py` | 호출수·비용·시간. 초과 시 강제중단 아니라 이후 호출 생략 + `degraded` |
| 스키마 버전·옛 기록 호환 | ✅ | `app/services/migrate.py` | 읽기 시점 정규화(멱등) |

---

## 알려진 한계 (구현하지 않기로 판단한 것 포함)

### 사실 검증의 범위

**검색 요약 근거와의 일치 여부만 판정한다. URL 원문의 사실성은 재검증하지 않는다.**
`verification_result.verification_scope == "search_snippet_only"` 로 코드가 이 사실을 들고 다니고,
화면·내보내기 문서·실행 JSON 이 같은 문구를 쓴다(`app/services/reliability.py` 단일 소스).

- `unsupported` 는 **'거짓'이 아니다** — 수집한 근거에서 확인하지 못했다는 뜻이다.
- 판정별 개수와 주장·근거 연결은 KOSENA 본문의 **부록**에 싣는다(`kosena_doc._verification`).
- ⚠️ **같은 문서를 반복 판정하면 `fact_support_rate` 가 0.2~0.9 로 벌어진다**(verify 가 주장을
  매번 새로 추출). `quality_gate` 가 이 값을 쓰므로 게이트 판정에 운이 섞인다 — 미해결 과제다.

### 출처는 '실행 전체' 기준이다

`sources_cited` 검사는 레지스트리에 URL 이 있는지만 본다. TAM/SAM/SOM·경쟁사 정보에 **직접
연결된 근거가 없어도 충족**이 된다. 14섹션 기획서 쪽은 `verify` 가 주장별 `evidence_ids` 를
붙이지만, **KOSENA 산출물의 항목별 근거 연결은 미구현**이다.
→ "각 KOSENA 주장별로 출처를 검증한다"고 말하면 **틀리다.**

### 본문 분량 (A4 30~50쪽 vs 약 11.9쪽)

**분량을 위한 분량은 넣지 않았다.** 같은 내용을 늘려 쓰거나 표를 쪼개 쪽수를 맞추는 것은
평가표의 '문서 완성도'(논리·시각화·인용)와 오히려 어긋난다. KOSENA 가 상정하는 30~50쪽은 팀이
**1차 자료(인터뷰·설문)와 부록·근거 자료를 붙인 분량**으로 보이며, 이는 자동 생성으로 메울
성질이 아니라 사람이 리서치 결과를 덧붙여 채우는 부분이다. 도구는 그 골격을 제공한다.

### 1차 자료를 만들 수 없다

평가표 '고객 이해' 우수 기준은 *인터뷰 + 설문*이다. AI 파이프라인은 이를 만들 수 없고, 지어내면
KOSENA 원칙(정량 주장은 1차 자료로 뒷받침)에 정면으로 위배된다. 따라서 목표를 '충족'이 아니라
**가설임을 명시하는 것**으로 두고, 그 표기 여부 자체를 검사한다(`hypothesis_labeling`).

### 이력 저장은 비영속이다

SQLite 파일(`data/projects.db`)에 저장한다. **Cloud Run 컨테이너는 재시작 시 파일이 사라지므로,
공개 배포에서 이력은 보존되지 않는다.** 사용자에게 산출물 다운로드를 안내하는 것으로 대응했다.
PostgreSQL 등 외부 DB 전환은 이번 제출 범위에서 제외했다(변경 위험 대비 이득이 작다).
⚠️ **GCS FUSE + SQLite 조합은 파일 잠금 문제로 금지**다(파일 손상).

### 프런트엔드에 CDN·빌드 도구를 쓰지 않았다

`app/static/index.html` 은 인라인 CSS/JS 단일 파일이다. 이유는 세 가지다:
FastAPI 가 그대로 서빙해 **빌드·CORS·별도 배포가 불필요**하고, 오프라인·폐쇄망에서도 동작하며,
외부 CDN 은 **가용성·버전 변동·공급망 위험**을 끌어들인다. 백엔드가 이 프로젝트의 주인공이라
프런트엔드 복잡도를 의도적으로 낮게 유지했다.
⚠️ 대가: **JS 자동 테스트가 없다**(이 환경에 node 도 없다). UI 변경은 브라우저 수동 확인에
의존하며, 실제로 테스트 전부 통과 상태에서 화면이 깨진 적이 있다.

---

## 미구현 — 후속 범위

| 항목 | 왜 지금 아닌가 |
|---|---|
| 검색 결과 최신성 메타(`retrieved_at`·`published_date`·기간 필터) | 근거 스키마를 건드려 `evidence_registry` 왕복·Artifact `evidence_ids` 에 파급 |
| Actions 승인형 CD(GCP 인증 → Cloud Run 배포) | `deploy-staging.yml` 에 `workflow_dispatch`·`concurrency`·build→health→smoke 까지는 있고, **호스트 배포 스텝만 확장점으로 비워 둠**. 시크릿을 다루는 변경이라 동결 직전 위험 |
| KOSENA 산출물 항목별 근거 연결(`evidence_ids`) | 코드가 임의로 "이게 TAM 근거겠지"를 추론해 부록을 만들면 **그것도 허위**다. Agent 출력에 id 를 받고 레지스트리에 실존하는 것만 남기는 방식이 필요 |
| Agent `_validate` 의 배열 원소 타입 검증 | 지금은 표시 계층(`displayText`)에서 흡수. 데이터 계층에서 강제하면 downstream Agent 가 받는 설명이 사라지고 Artifact parity·비교실험 기준선이 흔들림 |
| URL 원문 대조(Tier 3) | 크롤링·추출 신뢰성·비용 문제. 검증 범위를 넓히는 대신 **현재 범위를 정직하게 표기**하는 쪽을 택함 |
| 선택적 재실행(변경된 Agent 만) | `depends_on` 검증까지는 끝났고 실행 연결은 후속 |
| 사람 기획서 기준선 보정 | 사람이 쓴 기획서 3~5건이 필요(데이터 확보 문제) |
