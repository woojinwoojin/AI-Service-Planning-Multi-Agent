# 개발 로드맵 v2 — 평가 우선 · CI 상시 · Multi-Agent 개혁

> 작성 기준 커밋: `main @ ce7049e` (2026-07-22). 이 문서는 사용자가 제안한 로드맵을 **현재 저장소 상태에 맞춰 정정·구체화**한 것이다.
>
> **두 가지 핵심 원칙**
> 1. **평가는 두 번 나눈다** — 개편 *전* '비교 기준선', 개편 *후* '서비스용 품질 게이트'. 그래야 구조 개편이 실제 품질을 올렸는지 판단할 수 있다.
> 2. **CI는 지금부터 상시**, Production 배포만 마지막. CI/CD를 '마지막 한 번'으로 보면 안 된다.

---

## 진척 현황 (2026-07-22 업데이트)

**"지금 당장 할 3가지" — 전부 완료 ✅**

| # | 작업 | 결과 | PR |
|---|---|---|---|
| 1 | CI 상시 신설 (트랙 A) | ruff + pytest 자동화, action v5/v6 | #52 ✅ merged |
| 2 | baseline-v1 커밋 고정 (Phase 0) | 실측 24회 → `docs/baseline-v1.md`·`.json` | #54 (open) |
| 3 | 평가 세트·루브릭 승격 (Phase 1) | 12주제·8기준·변동성·사람보정 | #55 (open, #53 재착륙) |

- **CI가 실제 버그 포착**: `test_routes.py` 2건이 로컬 `.env`에 은닉 의존 → hermetic 수정. 평가 테스트도 실 API 호출 위생 문제 발견 → `USE_DUMMY=1` 고정.
- **baseline-v1 실측**(24회, gpt-4o-mini @ `ce7049e`, ~$0.29): wall 중앙값 직렬 127.4s→병렬 106.8s(**-16.1%**), p95 -17%, analysis_block -41%, 품질 동등(구조 100%·fallback 0), 근거율 85.3%(직렬 87.7·병렬 82.9), **재작성 실행률 100%** → 다음 병목=문서 재생성(PR-7 타깃).
- ⚠️ **stacked-PR 교훈**: #53을 CI 브랜치 위에 쌓았다가 #52가 먼저 main에 머지되어 평가 변경이 붕 뜸 → #55로 main 재착륙. **이후 독립 작업은 main 기준 브랜치로.**

**다음 진입점:** ~~Evidence Registry(2-1)~~ ✅ → ~~PR-7(2-3/2-4)~~ ✅ → ~~신뢰도 Tier 2 핵심~~ ✅ → ~~Tier 2 GT 스모크셋~~ ✅ → **Tier 3(원문 Extract) 또는 Phase 4(품질 게이트)** 또는 **2-2 Artifact Contract 백필**.
- **실측 완료 (2026-07-24, gpt-4o-mini, 소표본):** PR-7 성능(재작성 24.4→8.5s ≈ −65%, section 12/12) · Tier 2 GT(허위 통과 **0/4**·반대 근거 2/2·유형 9/10) · 평가 리포트(총점 **81.8**/100, σ1.7). → `docs/parallel_bench_result.md`·`docs/gt_report.md`·`docs/eval_report.md`.
- 남은 잔여(선택): 표본 확대 실측(6주제×2, baseline 동일 서명) · **사람 기획서 3~5건 보정**은 데이터 필요분으로 보류.

---

## 0. 현재 상태 스냅샷 (정확히)

| 영역 | 상태 |
|---|---|
| 워크플로 병렬화 | ✅ 직렬/병렬 그래프 보존 · fan-in join · 단계별 latency 계측(`timing.py`) · 비용/토큰/fallback 측정 · `WORKFLOW_MODE` Feature Flag (PR #41~#46) |
| 병렬 State 안전성 | ✅ `logs`/`timing_events` reducer, usage 병렬 누락 없음·동시성 검증(테스트 고정) |
| 비교 도구 | ✅ `run_parallel_bench.py`(AB/BA·이어하기·실험 서명·`--fresh`), `run_compare.py`(단일 vs 멀티, LLM judge 5기준) |
| 신뢰성 Tier 1 | ✅ `verification_scope="search_snippet_only"`, `reliability` 한계 문구, 실제 검색 출처만 인용, competitor 검색근거 보존 |
| 입력 자동완성 | ✅ 입력 보존·빈 항목만·비교 모드·추천 이유/확신도 (PR #48~#50) |
| 테스트/정적검사 | ✅ pytest 146(평가 8 포함, #55 머지 후) · ruff |
| 평가 세트·루브릭 | ✅ 고정 12주제·8기준 루브릭·심판 변동성·사람 보정 경로 (`eval_set.py`·`evaluation.py`·`run_eval.py`, PR #55) — 실측 리포트·사람 보정은 API 필요분 보류 |
| **CI** | ✅ **가동** (`.github/workflows/ci.yml`: PR/main push마다 ruff+pytest, PR #52) — 남은 것: 브랜치 보호에 required check 지정 |
| **baseline-v1 고정** | ✅ 커밋 고정 (`docs/baseline-v1.md`·`docs/baseline-v1.json`, 24회 실측·서명·재현명령, PR #54) |
| PR-7 섹션 단위 수정 (2-3/2-4) | ✅ 구현 (`feat/pr7-section-revise`): 구조화 issues·`sections.py` 파서/조립기·`section_revise`+full-revise fallback·라우팅·재작성 계측, 테스트 29건·190 passed. 성능 실측은 baseline 동일 벤치로 별도 예정 |
| API/State 계약 | ⚠️ 스키마 버전·DB migration 없음(SQLite JSON blob), `docs/ARCHITECTURE.md` 뒤처짐 |

### 확정 baseline-v1 (6주제·24회, gpt-4o-mini, `ce7049e`) — `docs/baseline-v1.md`
- 전체 wall 중앙값: 직렬 127.4s / 병렬 106.8s (**-16.1%**), p95 -17%(149.0→123.7s)
- 병렬 병목: `analysis_block` 35.7→21.0s(-41%), 이후 병목은 **문서 재생성 구간**(병렬 기준 revise 24.4s + polish 17.0s + draft 16.7s ≈ 58s = 전체의 절반 이상)
- 품질·비용 동등(14섹션 100%·순서 100%·PESTEL 100%·fallback 0·전부 success, 출처 9.0, 토큰 -1.5%), coverage ≈ 1.0
- 근거 확인율 85.3%(직렬 87.7 / 병렬 82.9), **재작성 실행률 100%**(24/24) → PR-7 절감 잠재력 큼
- (이전 3주제 스모크 -13.5%는 표본이 작아 흔들렸고, 24회 AB/BA에서 -16.1%로 안정화)

---

## Phase 0 — 병렬화 마무리 & 기준선 고정 ✅ 완료 (PR #54)

- `baseline-v1`을 **커밋으로 고정** 완료: `run_parallel_bench.py --topics 6 --reps 2 --fresh`(24회) 결과를 `docs/baseline-v1.md`(요약·서명·재현명령·해석) + `docs/baseline-v1.json`(원자료)로 커밋. 서명=git `ce7049e`·gpt-4o-mini·temp 0.3·topics_hash·reps·workflow version(프롬프트는 커밋이 핀).
- 이후 모든 개선은 이 기준선과 **동일 벤치**로 전후 비교한다(트랙 C: 프롬프트 변경 후 직접 비교 금지).

**완료 게이트:** ✅ `docs/baseline-v1.md` 커밋 + 재현 명령·서명 문서화됨.

> 기록할 기준선 항목: 전체 wall time · Agent별 latency · 평균 비용/토큰 · 구조적 품질(14섹션·빈 섹션·출처 수) · Reviewer 점수 · 근거 확인율.

---

## Phase 1 — 평가 기준선 구축 (+ 기본 CI 즉시)  ★개편 前 평가 — ✅ 인프라 완료 (PR #55)

**목표:** 구조를 크게 바꾸기 *전에*, "정말 좋아졌는가"를 측정할 최소 평가 기반을 만든다.

**작업(구현 산출물 = `docs/평가_루브릭_v1.md`):**
- ✅ **고정 평가 세트 12개 주제** + 순서 고정('시드') — `eval_set.TOPICS`(안정 id).
- ✅ **루브릭 명문화(8기준)**: 문제 정의 · 고객 구체성 · 시장 분석 · 경쟁 차별성 · 수익 모델 · 실행 가능성 · 논리 일관성 · 근거 활용 — `eval_set.RUBRIC` + `EVAL_JUDGE`(버전 상수).
- ✅ **결정론적 구조 검사 재사용**: `structural_quality`를 `evaluate_topic`에 통합.
- ✅ **비용·latency·버전 기록** + 실험 서명(rubric/prompt 버전 포함).
- ✅ **평가 모델 변동성 측정**: 심판 N회 → `total_stdev`(같은 문서 N회 채점 분산).
- ✅ **사람 기준선 경로**: `calibrate_against_human`·`run_eval.py --human`. → 실제 사람 기획서 3~5건 확보·1회 보정은 **보류**(API 필요).
- ⏳ **실측 eval 리포트**: `python run_eval.py`(실 LLM 비용) 후 `docs/eval_report.md` 커밋 — **보류**.

**[정정]** 이미 있는 자산을 신규 구축이 아니라 **'평가 세트+루브릭+변동성+사람 보정'으로 승격**함. 발표용 `compare.CRITERIA`(5기준)는 그대로 두고 서비스용 8기준을 별도 버전으로 분리(트랙 C).

**완료 게이트:** ✅ 고정 세트·루브릭·사람 보정 경로·변동성 측정 커밋. ⏳ 실측 `eval` 리포트 생성만 잔여(API 필요).

**CI (병행 도입 완료):** '상시 트랙 A' 참조 — PR #52로 GitHub Actions 가동.

---

## Phase 2 — Multi-Agent 구조 개혁 (가장 큰 백엔드 개편)

> 동적 Supervisor를 한 번에 만들지 않는다. '자유 대화'가 아니라 **작업(Artifact)과 근거(Evidence)를 주고받는 통제 가능한 협업**으로 간다.

- **2-1 Evidence Registry** — 모든 실제 검색 결과를 공통 저장소로: `{evidence_id, source_agent, query, url, snippet, used_by_claims[]}`.
  - [현황] `research.source_objects` + `competitor_sources`가 이미 부분 근거. 이를 **단일 레지스트리로 통합**하는 작업.
- **2-2 Artifact Contract** — 각 Agent 결과를 표준 객체로: `{artifact_id, owner_agent, artifact_type, evidence_ids[], content}`.
- **2-3 Reviewer Issue 구조화 ✅** — 점수+자유 피드백 대신 `{target_section_id, issue_type, severity, revision_instruction}`. (`feat/pr7-section-revise`)
- **2-4 섹션 단위 수정 ✅** — 전체 재작성 대신 문제 섹션만 담당 Agent가 보완. **full-revise fallback 유지**. 신규 `sections.py`(파서/조립기, 미수정 섹션 byte 동일), `draft_writer.section_revise`, `workflow._route_revision` 3분기. (`feat/pr7-section-revise`)
- **2-5 제한된 동적 실행** — Agent가 근거 부족을 '발견했을 때만' Research 추가 호출(예산 상한 안에서).

**[통합]** 2-3 + 2-4는 이미 상세 계획이 있는 **PR-7**(`docs/병렬화_측정결과_및_PR7_계획.md`)과 동일 축이다. 순서 제안: **2-1 Evidence Registry → 2-2 Artifact → (PR-7) 2-3/2-4 → 2-5**. Evidence Registry를 먼저 해야 2-3의 `evidence_id` 연결과 Phase 3(신뢰도)가 재작업 없이 이어진다.

**완료 게이트(단계별):** 각 하위 단계 후 **Phase 1 평가 세트로 비열등성 확인**(구조 품질·비용 하락 없음) + baseline 대비 latency 기록.

---

## Phase 3 — 정보 신뢰도 고도화 (Evidence Registry와 **함께** 설계)

> 신뢰도는 개편 후 붙이는 부가기능이 아니라 Evidence Registry와 같은 축. Tier 1은 유지.

- **Tier 2 ✅(핵심 구현, `feat/tier2-claim-types`)**: 문장을 `fact | inference | proposal`로 분류 → **사실 주장만** 검증 → 각 주장에 `evidence_id` 연결(2-1b) → '근거 미확인(unsupported)'과 '반대 근거(contradicted)'를 분리 → 소규모 Ground Truth 평가(후속).
  - [완료] verifier claim에 `claim_type` 추가, status에 `contradicted`·`not_applicable` 확장(비-사실은 검증 대상 아님). 집계 `fact_support_rate`·`evidence_link_rate`(완료 게이트 지표)·`claim_type_counts`·`contradicted` 목록. 같은 LLM 콜 재사용(추가 비용 0). UI 배지·bench 분포 반영. 테스트 7건.
  - [완료] **GT 스모크셋(10건, 균형 구성)** 코드·세트·CLI 구현(`gt_eval`·`verifier.judge_claim`·`run_gt_eval.py`, `feat/tier2-gt-eval`). 허위 통과율 등 지표를 n/N 로 산출. **실측 리포트만 API 비용분 보류.**
- **Tier 3**: URL 원문 Extract → 주장 ↔ 원문 문장 직접 비교 → 최신성 검사 → 독립 출처 여부 → 출처 간 충돌 탐지.

**추천 순서(중요):** Evidence Registry(2-1) → 주장-Evidence 연결(2-3/Tier2) → 원문 Extract → 정식 사실성 검증(Tier3). **Tier 3를 Evidence 구조 전에 하면 나중에 다시 갈아엎게 된다.**

**완료 게이트:** Tier 2 = 주장별 `evidence_id` 연결률·사실 주장 검증률이 리포트에 표기 ✅(`evidence_link_rate`·`fact_support_rate`를 verification_result·bench에 노출; **실측 리포트는 GT 후속 PR**). Tier 3 = 원문 대조 표본에서 정확도 측정.

---

## Phase 4 — 평가 프로세스 최종 (서비스용 품질 게이트)  ★개편 後 평가

**목표:** 단순 점수가 아니라 **출력 가능 여부를 결정하는 게이트**.

```python
release_ready = (
    score >= calibrated_threshold
    and critical_issues == 0
    and major_issues <= 1
    and structure_valid
    and evidence_coverage >= 0.8
)
```

**작업:** ~~release_ready 게이트 + 미해결 이슈 표면화~~ ✅(`feat/phase4-quality-gate`) · ~~수정 전후 후보 버전 비교 → 나빠지면 이전 최고 버전 유지~~ ✅(`feat/phase4-best-version`) · ~~최대 수정 라운드 제한~~ ✅(자동 재작성 1회 상한, `_route_revision`의 `revision_count<1`) · ~~평가 모델 변동성 측정~~ ✅(eval `total_stdev`) · ~~Writer/Reviewer 역할(모델) 분리~~ ✅(`feat/phase4-reviewer-model`) · 사람↔LLM 평가 상관 측정 · ~~미해결 이슈를 사용자에게 표시~~ ✅.

- **✅ 게이트 1차 구현**: `app/services/quality_gate.py`(`evaluate` → `release_ready`·`checks`·`blocking_reasons`·`metrics`·`unresolved_issues`). 기준(잠정): 총점≥80·치명 이슈 0·주요 이슈≤1·서식 정상·근거 충족률(fact_support_rate)≥0.8. state/RunResult/저장/UI에 표면화. 테스트 10건.
- **✅ 최고 버전 채택**: `workflow._select_best`(final_reviewer→**select_best**→verify). 재작성본(final_review) < 초안(initial_review)이면 초안을 최종본으로 되돌리고 표시 점수·검증도 초안 기준으로 정정. state `best_version`·`reverted_from_revision`, UI '초안 채택' 안내. 수동 /revise는 제외(사용자 의도 존중). 테스트 5건.
- **✅ Writer/Reviewer 모델 분리**: `reviewer._reviewer_model`(reviewer/final_reviewer 가 작성 모델 대신 심판 모델 사용). 소스=API `reviewer_model` 필드 또는 env `REVIEWER_MODEL`, 미지정 시 작성 모델로 폴백(회귀 없음). state `reviewer_model`·_RUN_KEYS 기록, 분리 시 로그 '심판 모델 분리' 표기. 테스트 4건.
- **[후속(데이터 필요)]** calibrated_threshold(사람 기획서 3~5건 보정) · 사람↔LLM 상관.

**완료 게이트:** 게이트 통과/미통과가 State·응답에 표면화되고, unresolved issue가 UI 계약에 포함. → **1차 충족**(임계값은 사람 보정 전 잠정값).

---

## Phase 5 — API·State·Streaming 계약 안정화 (UI 前 필수)

> UI를 먼저 만들면 백엔드 State가 바뀔 때마다 화면을 반복 수정하게 된다.

안정화 대상 필드: 실행 진행 상태 · Agent별 산출물 · 최종 기획서 · 평가 결과 · 검증 결과 · 출처 목록 · 수정 이력 · 비용/latency · 실패/fallback 상태 · (신규) release_ready·unresolved issues.

**작업:** ~~응답 스키마 **버전 관리**~~ ✅(`feat/phase5-state-versioning`) · ~~**DB migration**(기존 프로젝트 ↔ 새 State 호환)~~ ✅(읽기 시점 정규화) · 오류 응답 형식 통일 · **Streaming 이벤트 계약 확정** · OpenAPI 문서 정리 · **`docs/ARCHITECTURE.md` 최신화**(현재 뒤처짐).

- **✅ State 버전 + 옛 기록 호환**: `app/services/migrate.py`(`STATE_VERSION`·`upgrade_state`). SQLite JSON blob 이라 DDL migration 대신 **읽는 시점 정규화** — `store.get_project` 가 옛 기록의 누락 필드에 안전 기본값 주입 + `quality_gate` 소급 계산 + `verification_summary` 채움 + 버전 태깅(멱등). 실행 종료(`_finalize_run`·`rerun_finalizers`)에도 태깅. RunResult·_RUN_KEYS 에 `state_version`, FastAPI `version=0.2.0`·설명에 스키마 버전 명시. 테스트 8건(정규화·게이트 소급·멱등·재조회 회귀).
- **✅ 오류 응답 형식 통일**: `app/api/errors.py`(`register_error_handlers`) — HTTPException·RequestValidationError(422)·미처리 Exception(500)을 단일 봉투 `{"error":{code,message,status,details?}}`로 통일. status→안정 code 매핑, 500은 내부 상세 미노출(로그만). `ErrorResponse` 스키마·테스트 5건.
- **✅ Streaming(SSE) 이벤트 계약 확정**: `/run/stream` 이벤트를 `start → node* → (done|error)`로 명문화. `run_workflow_stream`이 `{"type":"start","workflow_mode":…}` 선두 방출, `error`는 HTTP 오류 봉투와 동일 구조(`{type,message,error:{code,message,status}}`)로 내부 상세 미노출(로그만), `message`는 UI 하위호환 유지. 소비자는 모르는 type 무시(전방 호환). 테스트 2건(+기존 보강).
- **✅ OpenAPI 정리**: FastAPI `openapi_tags`(실행/이력/입력 보조/내보내기/시스템)로 엔드포인트 그룹화, 라우트별 `summary`. 공통 오류 응답(422·500)을 `ErrorResponse` 스키마로 노출, `/projects/{id}` 404·`/suggest` 400·`/run/stream` `text/event-stream` 문서화. 테스트 5건.

**Phase 5 완료 게이트: 충족.** 버전된 OpenAPI(v0.2.0·태그·오류/SSE 스키마) + migration(읽기 정규화) + 옛 프로젝트 재조회 회귀 테스트 통과. (오류 응답 통일·SSE 계약·문서 동기화 포함.)

**완료 게이트:** 버전된 OpenAPI + migration(읽기 정규화) + 옛 프로젝트 재조회 회귀 테스트 통과 → **1차 충족**(스키마 버전 노출·`upgrade_state`·회귀 테스트 8건). 잔여: 오류/스트리밍 계약·ARCHITECTURE.

---

## Phase 6 — Lovable 기반 UI 개선

내부 Agent 구조를 과도하게 노출하지 않고 **가치 중심 경험**으로. "14개 Agent 실행 중" → "시장 근거 수집 / 전략 구성 / 품질 검토" 같은 단계 언어.

- 입력: 아이디어 한 줄 · 목표 고객 · 시장 · 제약 · 문서 목적
- 실행: 근거 수집 → 분석 → 전략 → 작성 → 검토·보완 (단계 언어)
- 결과: 최종 기획서 · 품질 검토 상태 · 주요 출처 · **추가 확인 필요 항목** · 섹션별 수정 · DOCX/PPTX/MD 다운로드

**진행:**
- **✅ 진행 화면 단계 언어화**(`feat/ui-progress-stages`): 개별 노드 나열 → 5단계(`근거 수집 → 시장·고객·환경 분석 → 기획서 작성 → 검토·보완 → 근거 검증`) 그룹으로 표시(pending/active/done). 17개 노드를 STAGES 로 매핑(누락됐던 section_revise·select_best 포함), 진행바·ETA 로직 유지. 표시 전용 변경(데이터·결과 무영향).
- **[후속]** 결과 화면 정보구조·입력/수정 흐름·전체 시각 폴리시.

**완료 게이트:** Phase 5 계약만 소비(백엔드 추가 변경 없이 동작).

---

## Phase 7 — E2E · 보안 · 부하 · 사용자 테스트

- **E2E**: 입력→생성→수정→다운로드 전 흐름 자동화.
- **부하**: 동시 실행 시 rate limit·timeout·비용 급증 관측(예산 상한과 연계).
- **사용자 테스트(5~10명)**: 결과를 그대로 활용 가능한가 · 가장 부족한 섹션 · 출처 설명 이해도 · 수정 편의 · 대기시간 수용성 · 재사용 의향.
- 보안 항목은 '상시 트랙 E'에서 지속.

---

## Phase 8 — Staging CD → Production CD

- **Staging CD (API 안정화 후):** `main merge → Docker build → Staging 배포 → health check → smoke test`.
- **Production CD (UI·E2E 후):** `release tag → Production 배포 → health check → 실패 시 rollback`.

---

## 상시(Cross-cutting) 트랙 — 특정 단계에 묶이지 않고 계속

### A. CI ✅ 가동 (PR #52)
`.github/workflows/ci.yml` — PR/main push마다 **ruff + pytest**(실 LLM 미호출·API 키 불필요). 동시성 취소·타임아웃·최소 권한, action checkout@v5/setup-python@v6.
- ✅ 완료: ruff check · pytest(146) · mock workflow·schema 회귀·병렬 State 충돌은 기존 pytest가 커버.
- ⏳ 남은 확장: **브랜치 보호에 required status check 지정**(= 테스트 실패 시 merge 차단) · coverage 하한 · dependency 취약점 · secret 스캔 · Docker build.
> [현황] `pytest 146 · ruff` 통과 상태로 자동화됨(리스크 낮음, 효과 큼). CI가 실제로 은닉 `.env` 의존 버그 2건을 잡아냄.

### B. 관측성·운영 모니터링
usage/latency 확장: Agent별 성공·실패율 · 모델별 토큰/비용 · 검색 실패율 · 품질 점수 분포 · 평균 수정 횟수 · 90점 미만 비율 · 요청별 Trace. (Langfuse는 이미 배선되어 있어 여기서 효과 큼.)

### C. 모델·프롬프트 버전 관리 / 재현성
결과 원인 추적을 위해 실험 서명에 포함: `git commit · workflow version · prompt version · model · temperature · 검색 설정 · rubric version`.
> [현황] ✅ `evaluation.experiment_signature`에 **rubric/prompt version 추가** 완료(평가 축). `parallel_bench`는 git commit이 프롬프트를 핀(baseline-v1). 프롬프트 변경 후 옛 벤치와 직접 비교 금지.

### D. 비용·시간 예산 정책
실행별 상한: 최대 병렬 Agent 수 · 최대 추가 검색 · 최대 수정 라운드 · 최대 LLM 호출 · 최대 비용 · 최대 wall time. (동적 실행(2-5) 도입 전 **반드시** 선행.)

### E. 보안·사용자 데이터
API 키 서버 보관 · 입력 길이 제한 · Rate limit · **Prompt injection 방어**(검색 결과 `<검색결과>` 격리는 이미 있음) · 악성 URL 처리(출처 링크 http(s) 검증은 이미 있음) · 개인정보 입력 경고 · 로그 내 PII 제거 · CORS/인증.

### F. 문서 동기화
`docs/ARCHITECTURE.md`(뒤처짐)·README를 각 구조 개편 후 갱신. 발표/리뷰 전 필수.

---

## 정정·보강 요약 (사용자 초안 대비)

1. **[해결] CI** — PR #52로 상시 가동(ruff+pytest). 남은 것은 브랜치 보호 required check 지정.
2. **[해결] Phase 0 baseline** — PR #54로 `docs/baseline-v1.md`·`.json` 커밋 고정 완료.
3. **[통합] Reviewer Issue 구조화 + 섹션 단위 수정 = 기존 PR-7 계획** — 별도 취급 말고 Phase 2-3/2-4로 통합, 단 **Evidence Registry(2-1)를 먼저**.
4. **[보강] 예산 정책(트랙 D)은 동적 실행(2-5)의 선행 조건** — 순서 의존성 명시.
5. **[보강] 문서 동기화(트랙 F)** 추가 — `ARCHITECTURE.md`가 코드보다 뒤처져 있음.
6. **[유지] 두 핵심 원칙** — 평가 2회 분리, CI 상시.

---

## 지금 당장 할 3가지 — ✅ 전부 완료 (2026-07-22)
1. ✅ **CI 워크플로 신설** (`.github/workflows/ci.yml`: ruff+pytest) — PR #52. *(상시 A)*
2. ✅ **baseline-v1 커밋 고정** (24회 실측 + 서명 + 재현명령) — PR #54. *(Phase 0)*
3. ✅ **평가 세트·루브릭 승격** (12주제 + 8기준 + `structural_quality` 통합 + 변동성 + 사람 보정 경로) — PR #55. *(Phase 1)*

**다음 세션 진입점:** ~~Evidence Registry(2-1)~~ ✅ · ~~PR-7(2-3/2-4)~~ ✅ · ~~신뢰도 Tier 2 핵심(claim_type·contradicted·연결률/검증률)~~ ✅ 완료 → **Tier 2 GT 스모크셋(허위 통과율 n/N)** 또는 **Tier 3(원문 Extract)/Phase 4(품질 게이트)** 진입.
- 선행 정리(권장): 3개 PR merge 확인 · 브랜치 보호에 CI required check 지정 · (여력 시) 평가 실측 리포트 1회.
- 착수 팁: baseline-v1이 **문서 재생성(재작성 100%·병렬 58s)**을 병목으로 확정 → PR-7 효과 기대치 큼. 단 **Evidence Registry(2-1) 먼저**(2-3의 `evidence_id`·Tier 2가 재작업 없이 이어지도록).
